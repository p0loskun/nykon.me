// noinspection JSUnusedGlobalSymbols

/** Blog post properties type. */
export type BlogPostProps = {
  title: string;
  description: string;
  date: string;
  content: string;
};

/** Blog posts data. */
export const blogPosts = [];

export default blogPosts;
